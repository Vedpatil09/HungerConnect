<div
  class="food-card"

  data-id="<%= food._id %>"
  data-title="<%- food.title %>"
  data-desc="<%- food.description %>"
  data-qty="<%= food.quantity %>"
  data-loc="<%- food.location %>"
  data-exp="<%= food.expiry %>"
  data-img="<%= food.image %>"
  data-lat="<%= food.latitude || '' %>"
  data-lng="<%= food.longitude || '' %>"
  data-ai="<%- food.aiSuggestion || '' %>"
>

  <!-- Food Image -->
  <img src="<%= food.image %>" class="food-img" />

  <!-- Card Content -->
  <div class="food-info">

    <h3><%= food.title %></h3>

    <p class="desc"><%= food.description %></p>

    <div class="meta">
      📍 <%= food.location %> |
      ⚖️ <%= food.quantity %>
    </div>

    <div class="footer">

      <span class="status <%= food.status %>">
        <%= food.status %>
      </span>

      <span class="time">
        ⏰ <%= food.time || "Just now" %>
      </span>

    </div>

  </div>

</div>
