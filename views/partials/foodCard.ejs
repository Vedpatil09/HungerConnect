<div class="food-card">

  <!-- Food Image -->
  <img src="<%= food.image %>" class="food-img" />

  <!-- Card Content -->
  <div class="food-info">

    <h3><%= food.title %></h3>

    <p class="desc">
      <%= food.description %>
    </p>

    <div class="meta">
      📍 <%= food.location %> |
      ⚖️ <%= food.quantity %>
    </div>

    <div class="footer">
      <span class="status <%= food.status %>">
        <%= food.status %>
        <% if (food.status === "active") { %>
  <form action="/book/<%= food._id %>" method="POST">
    <button class="book-btn">Book</button>
  </form>
<% } %>

      </span>

      <span class="time">
        ⏰ <%= food.time %>
      </span>
    </div>

  </div>

</div>
